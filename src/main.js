require.config({
    paths: {
        jquery: '../components/jquery/jquery'
    }
});

require(['jquery'], function($) {
    $(function() {
        // TODO: draw some leds now
        // TODO: draw ide
        // TODO: eval
    });
});
